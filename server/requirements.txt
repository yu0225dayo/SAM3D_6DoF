# SAM3D_6DoF サーバ Python 依存パッケージ
# 対象環境: Python 3.11, CUDA 12.1
#
# インストール手順:
#   pip install -r requirements.txt
#
# ※ 以下のパッケージはビルドが必要なため別途インストール:
#   pip install pytorch3d  (要 CUDA + gcc, 数分〜十数分)
#   pip install gsplat     (要 CUDA + gcc)
#   pip install flash-attn (要 CUDA + gcc, 30分以上かかる場合あり)
#   pip install git+https://github.com/facebookresearch/sam2.git

# --- PyTorch (CUDA 12.1) ---
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# --- 数値計算・画像処理 ---
numpy==1.26.4
scipy==1.17.1
opencv-python==4.9.0.80
Pillow==11.3.0
scikit-image==0.23.1
open3d==0.18.0
trimesh==4.11.3
point-cloud-utils==0.29.5
pyrender==0.1.45
pymeshfix==0.17.0
pycocotools==2.0.7

# --- 深層学習フレームワーク ---
einops==0.8.2
timm==0.9.16
transformers==4.39.3
accelerate==1.13.0
peft==0.10.0
roma==1.5.1
fvcore==0.1.5.post20221221

# --- 3D 処理 (CUDA ビルド済みホイール) ---
spconv-cu121==2.3.8
cumm-cu121==0.7.11
kaolin==0.17.0

# --- SAM-3D / Hydra 設定 ---
omegaconf==2.3.0
hydra-core==1.3.2
hydra-submitit-launcher==1.2.0
easydict==1.13
rootutils==1.0.7
loguru==0.7.2
segment-anything==1.0

# --- サーバ (server.py) ---
fastapi==0.135.1
uvicorn==0.41.0
httpx==0.28.1
python-multipart

# --- 学習・可視化 (SAM-3D training) ---
lightning==2.3.3
tensorboard==2.16.2
wandb==0.20.0
seaborn==0.13.2
gradio==5.49.0

# --- ユーティリティ ---
PyYAML
tqdm
